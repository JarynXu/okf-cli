# Vendored SDK snapshot

This directory contains a build snapshot derived from `JarynXu/okf-sdk` commit `483f192e1e9197bb3f18f7809d51aeb9b4868ea1`. It bootstraps private-repository CI before the crate is published. Replace the path dependency with the released crate when available.
